# Backward-compatible developer/test install.
# Direct constraints live in requirements.in and requirements-dev.in.
# The production image installs requirements.lock instead.
-r requirements-dev.lock
